services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: schedule-generator
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
